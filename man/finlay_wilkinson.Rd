% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finlay_wilkinson.R
\name{finlay_wilkinson}
\alias{finlay_wilkinson}
\title{Finlay-Wilkinson Regression Analysis}
\usage{
finlay_wilkinson(data, gen, env, resp)
}
\arguments{
\item{data}{A data frame containing genotypes, environments, and trait matrix columns.}

\item{gen}{Unquoted column name designating variety/genotype labels.}

\item{env}{Unquoted column name designating trial environment labels.}

\item{resp}{Tidyselection expression identifying one or more columns to analyze
(e.g. \code{c(Yield, TKW)}, \code{starts_with("Trait")}, or \code{everything()}).}
}
\value{
A named list where each element represents an analyzed trait containing:
\itemize{
\item \code{estimates}: A tibble with variety names (\code{gen}), intercepts (\code{b0}), and slopes (\code{b1}).
\item \code{predictions}: A tibble showing aggregated inputs matched against expected regression fits (\code{yhat}).
\item \code{var_e}: A named numeric vector tracking residual variances for each genotype.
\item \code{var_e_weighted}: The pooled weighted error variance across the trait landscape.
}
}
\description{
\code{finlay_wilkinson()} evaluates Genotype-by-Environment (GxE) interactions across
multiple traits simultaneously using a tidyselection interface. Raw experimental
replicates within each genotype and environment combination are automatically
averaged into cell means prior to analysis.

The underlying phenotypic response is modeled per trait as:
\deqn{Y_{ij} = b_0 + b_1 h_j + \varepsilon_{ij}}

Where:
\itemize{
\item \eqn{Y_{ij}} is the mean phenotypic performance of genotype \eqn{i} in environment \eqn{j}.
\item \eqn{b_0} is the genotype intercept, representing its average performance across all trials.
\item \eqn{b_1} is the environmental sensitivity slope of genotype \eqn{i}.
\item \eqn{h_j} is the environmental index for site \eqn{j}, calculated via sum-to-zero contrasts (\eqn{\sum h_j = 0}).
\item \eqn{\varepsilon_{ij}} is the residual error variance associated with genotype \eqn{i} in environment \eqn{j}.
}
}
\examples{
\dontrun{
reg <- finlay_wilkinson(
  data_ge2,
  env  = ENV,
  gen  = GEN,
  resp = PH
)
plot(reg)

# Example 1: Use an expression helper inside the highlight argument
# Highlights all genotypes that are highly responsive to environmental
changes (slope > 1)
plot(reg, trait = "PH", highlight = "b1 > 1")

# Example 2: Combine expression filters
# Highlights genotypes that are highly stable (b1 < 1) AND produce yields
above the average population mean
plot(reg, trait = "PH", highlight = "b1 < 1 & b0 > mean(b0)")
}
}
\seealso{
\code{\link[=eberhart_russell]{eberhart_russell()}}, \code{\link[=ge_factanal]{ge_factanal()}},  \code{\link[=superiority]{superiority()}}, \code{\link[=ecovalence]{ecovalence()}}, \code{\link[=ge_stats]{ge_stats()}}
}
