% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finlay_wilkinson.R
\name{plot.FW}
\alias{plot.FW}
\title{Plot Method for Finlay-Wilkinson Multi-Trait Objects}
\usage{
\method{plot}{FW}(
  x,
  trait = NULL,
  type = 1,
  genotypes = NULL,
  highlight = 10,
  show_reference = TRUE,
  plot_theme = theme_metan_minimal(),
  ...
)
}
\arguments{
\item{x}{An object of class \code{FW} generated by \code{finlay_wilkinson}.}

\item{trait}{Character string specifying which trait in the list to plot.
Defaults to the first trait.}

\item{type}{Integer specifying the plot architecture type.
\itemize{
\item \code{1} (Default): Plots variety regression trajectories against the calculated
environmental index gradient (h).
\item \code{2}: Generates the Finlay-Wilkinson coordinate mapping model, tracking
environmental sensitivity slopes (\eqn{b_1}) against overall variety arithmetic means (\eqn{b_0}).
}}

\item{genotypes}{Optional selection tool. Can be a character vector of specific
genotype names, or a logical string expression evaluating parameters in the
estimates table (e.g., \code{"b1 > 1"}, \code{"b1 < 1"}, or \code{"b0 > mean(b0)"}).}

\item{highlight}{Dynamic highlighting tool. Pass an \strong{integer} to highlight the top
\eqn{n} genotypes with the largest slope (\code{b1}), a \strong{character vector} of
explicit genotype names, or a \strong{logical string helper expression} (e.g., \code{"b1 > 1"})
to highlight matching elements. Non-highlighted genotypes turn gray with alpha = 0.5.
Default is \code{10}.}

\item{show_reference}{Logical. If \code{TRUE}, adds a dashed reference line. For \code{type = 1},
plots the average population performance baseline (\eqn{\beta_1 = 1}). For \code{type = 2},
adds crosshair markers highlighting population static averages. Default is \code{TRUE}.}

\item{plot_theme}{The graphical theme of the plot. Default is
\code{theme_metan_minimal()}. For more details, see \code{\link[ggplot2:theme]{ggplot2::theme()}}.}

\item{...}{Unused trailing arguments for S3 consistency.}
}
\value{
A \code{ggplot} object tracking genotype performance lines across
the environmental index gradient.
}
\description{
Visualizes genotype regression lines across the calculated environmental index
for a specified trait, allowing custom variety filtering, flexible expression-based
highlighting, and population reference lines.
}
\references{
Finlay, K.W., and G.N. Wilkinson. 1963. The analysis of adaptation in a
plant-breeding programme. Australian Journal of Agricultural Research 14(6):
742–754. \doi{10.1071/AR9630742}
}
